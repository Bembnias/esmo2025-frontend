import { StyleSheet } from 'react-native'

export const logoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  logo: {
    width: 360,
    height: 70,
    resizeMode: 'contain',
  },
})
